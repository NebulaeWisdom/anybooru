
- 思考方式遵从第一性原理、奥卡姆剃刀原则。
- 开始任务前直接读 HANDOFF.md，不要询问是否要读。
- 重要发现（环境差异、隐藏依赖、易踩的坑、未验证结论、下一步入口）写入 HANDOFF.md。
- 优先使用正向编程，禁止防御性编程。
- 禁止参数硬编码和隐式兜底；重要参数应该提升为项目根目录参数文件，不使用环境变量方式注入参数。
- 禁止写安全护栏，禁止写测试，禁止 checksum。
- 使用项目内 venv 运行和测试项目。
- 网络请求使用代理 proxy-host:port。
- 必须使用 Git 管理项目进度。动手前按独立功能或修复划分提交边界，每完成一个独立变更就及时提交；每个 commit 只承担一个明确目的，必须便于单独审查、回退和挑选。禁止把多个可分离的功能、性能优化、重构或文档改动攒成难以拆分的超大 commit；存在依赖时按依赖顺序拆成小提交，不得以存在依赖为由全部打包。

